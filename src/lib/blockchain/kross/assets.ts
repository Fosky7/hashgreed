// src/lib/blockchain/kross/assets.ts
// FIX line 14: MARKETPLACE_CONFIG lives in config.ts, not the read-only deployed.config.ts
import { MARKETPLACE_CONFIG, toWavelets, fromWavelets, FEES } from "./config";

// ... rest of the file unchanged.
